#include <iostream>
using namespace std;
#define MAX 200100
int dp[]

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    int T; cin >> T;
    while(T--) {

    }
}